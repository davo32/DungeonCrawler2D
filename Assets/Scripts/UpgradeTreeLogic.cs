using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpgradeTreeLogic : MonoBehaviour
{
    public bool[] Unlocks = { false, false, false, false };
    public List<ActionCardLogic> UpgradePath = new List<ActionCardLogic>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
